# Standard Dockerfile - Multi-stage Build (Vulnerable by Default)

# Stage 1: Build
FROM node:24 AS builder
WORKDIR /app
# Copy package files from the app directory
COPY app/package*.json ./
RUN npm install
# Copy the rest of the app content
COPY app/ ./

# Stage 2: Production
FROM node:24
WORKDIR /app

# Copy from builder
COPY --from=builder /app /app

# Install standard dependencies if needed (none for this basic demo)
# RUN apt-get update && ...

EXPOSE 3000
CMD ["npm", "start"]
