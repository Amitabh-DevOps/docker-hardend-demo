# Standard Dockerfile - Multi-stage Build (Vulnerable by Default)

# Stage 1: Build
FROM node:24 AS builder
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .

# Stage 2: Production
FROM node:24
WORKDIR /app

# Copy from builder
COPY --from=builder /app /app

# Install additional tools often found in development/fat images (Increased attack surface)
RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*

EXPOSE 3000
CMD ["npm", "start"]
